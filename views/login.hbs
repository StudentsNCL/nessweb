<div class="container">
    <div class="row">
        <div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3">
            <form class="form-signin" role="form" method="POST">
                <legend>NESSweb</legend>
                <p>
                    Login to NESS using your university credentials.
                </p>
                {{#if failed}}<div class="alert alert-danger">We had trouble logging you in. Please try again.</div>{{/if}}
                <div class="form-group first">
                    <input class="form-control" type="text" name="id" placeholder="b20xxxxx" value="{{ness_id}}" id="user" required/>
                </div>
                <div class="form-group">
                    <input class="form-control" type="password" name="pass" id="password" placeholder="Password" required/>
                </div>
                <button class="btn btn-lg btn-primary btn-block" type="submit">Log In</button>
                <div class="checkbox">
                    <label>
                        <input type="checkbox" name="remember" checked="checked"> Remember Me
                    </label>
                </div>
            </form>
        </div>
    </div>
</div>
